# Copy to .env and fill in values. Never commit real secrets.
#
# Use canonical API key names — the code resolves the correct key
# based on the TRANSLATE_PROVIDER / EMBEDDINGS_PROVIDER / LLM_PROVIDER
# setting in your config.yml.

# LLM / Translation / Embeddings providers (set whichever you use)
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
GOOGLE_API_KEY=""
XAI_API_KEY=""
DEEPSEEK_API_KEY=""

# Hosted inference (Fireworks, Together, etc.)
FIREWORKS_API_KEY=""
TOGETHER_API_KEY=""

# YouTube Data API (rare fallback for discovery)
YOUTUBE_API_KEY=""
